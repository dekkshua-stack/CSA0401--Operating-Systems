#include <stdio.h>
#include <sys/ipc.h>
#include <sys/shm.h>
#include <string.h>
int main()
{
    int shmid;
    char *str;
    shmid = shmget(1234, 1024, 0666 | IPC_CREAT);
    str = (char *)shmat(shmid, NULL, 0);
    strcpy(str, "Hello! Shared Memory IPC");
    printf("Data written to shared memory: %s\n", str);
    printf("Data read from shared memory: %s\n", str);
    shmdt(str);
    shmctl(shmid, IPC_RMID, NULL);
    return 0;
}



OUTPUT:

Data written to shared memory: Hello! Shared Memory IPC
Data read from shared memory: Hello! Shared Memory IPC
